import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign Up - TradeSync",
  description: "Create your TradeSync account",
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
